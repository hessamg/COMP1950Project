
$(document).ready(function(){

	$("#nav").hide();
	
	
	/*
	if user clicks on the menu vector,
	fade out the menu icon #nav_icon, 
	slide open the full sized #nav
	*/
	$("#nav_icon").click(function(){
		$("#nav").slideToggle();

	
	/*
	if user clicks on the 'X' in the full sized menu,
	slide away the full sized #nav
	fade in the menu vector #nav_icon, 
	*/

	});

});